#include "main.h"
/**
  * _strcmp - compares two strings
  *@s1: compare
  *@s2: compare
  *Return: int
  */
int _strcmp(char *s1, char *s2)
{
	int i = 0;
	int j = 0;

	if (s1[i] || s2[j] == 0)
	{
		i++;
		j++;
	}
	return (0);
}
